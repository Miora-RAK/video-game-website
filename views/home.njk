{% extends "layout.njk" %}
{% block content %}
  <h1> Hello World! </h1>
   <p>
  {%for plateform in plateformsAccess%}
    <li>
      <img href = "/{{plateform.logo.url}}"src="{{plateform.logo.url}}" alt="{{plateform.name}}" />
      <a href="/{{plateform.id}}">{{plateform.name}}</a>
    </li>
  {%else%}
    <li> It is empty </li>
  {%endfor%}
  </p>
{% endblock %}


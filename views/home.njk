{% extends "layout.njk" %}
{% block content %}
  <h1> Welcome to the GAME CATALOGUE! </h1>
   <p>
  {%for plateform in plateformsAccess%}
    <li>
      <a href = "/{{plateform.logo.url}}"><img src="{{plateform.logo.url}}" alt="{{plateform.name}}" /></a>
      <a href="/{{plateform.id}}">{{plateform.name}}</a>
    </li>
  {%else%}
    <li> It is empty </li>
  {%endfor%}
  </p>
{% endblock %}

